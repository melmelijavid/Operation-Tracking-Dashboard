// Placeholder. The real listing/search/edit UI lands in the next step of
// Phase 2 once the GET /api/admin/users endpoint is in place.
export default function AdminUsersPage() {
  return (
    <div className="admin-placeholder">
      <h2>User Management</h2>
      <p>Listing, search, filter, and edit controls land in the next commit.</p>
    </div>
  );
}
